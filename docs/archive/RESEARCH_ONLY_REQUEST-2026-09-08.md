# Archived milestone request (historical)

> This file records the completed 2026-09-08 research-only milestone. It is not current setup or operational guidance; see `README.md` and `HANDOFF.md`.

# Civicfolio: finish the research-only product

User explicitly authorizes removing all brokerage functionality and making a fully usable localhost research agent. Astra has already POSTed /api/robinhood/disconnect and verified connected:false. Do not read token/credential files, reconnect, revoke using guessed endpoints, place trades, or send any private financial data anywhere. Do not touch FairwayOS. Current main starts at 141b40d, clean. Preserve existing user research/chat/journal data. No reset/demo reseeding. Use this project only. You are the GLM implementation worker; do not delegate or use browser tools. Astra handles browser review. No commits/push until Astra reviews.

Implement the following cohesive bounded milestone. Existing packages/runtime only unless a dependency is genuinely essential. Use tests with mocks and isolated temp storage; no actual model/network calls in unit tests. Do not rerun paid LLM calls automatically. No recurring jobs.

1. REMOVE real brokerage integration entirely: server/src/robinhood.ts, alerts.ts (broker related), src/pages/TradePanel.tsx and scripts/debug-rh-*.mjs. Remove imports, API client order/connect/alert methods and UI. All /api/robinhood routes and /robinhood/callback must return a static JSON 410 with execution_enabled:false; never reflect URL input, read credentials, call remote brokers, or redirect into authorization. Retain an explicit disabled status if useful, consistent meta/settings. No execution tool to the research model. Do not recreate broken order code. Legacy paper data may stay on disk; do not show it as actual positions or user's buying power. Prefer no portfolio/trading UI. Add research-only banner, source freshness, settings/status panel, external-processing notice (Ollama cloud can process prompts even via localhost), functioning ticker search and stock detail. Current source has delayed/unofficial Yahoo endpoints and SEC filed fundamentals. Do not call them exchange-quality real-time or guarantee continuity.

2. RESEARCH QUALITY: rewrite llm.ts prompt to direct, useful evidence-backed research. A reasoned hypothesis/bull/bear view is fine, but never force buy decisions on thin/missing/stale data, guaranteed returns, unsupported sizing or high confidence. Remove 'never say no live data', 'quotes were fetched' assumptions and 'still make the call'. Make actual successful/failed quote/news/search/fundamentals availability and timestamps visible to model and response/UI. If all sources fail do not invoke model to hallucinate; return a useful unavailable/retry response. Quotes.as_of missing must remain unknown, NOT fetched-now. Mark delayed/cached data honestly. Evidence URLs must be http(s) and model-provided arbitrary links must not be promoted to vetted citations. Label supplied references as retrieved references, not proof every claim verified. Match numeric levels to anchors means anchor match, NOT validated prediction. Make confidence explicitly qualitative, not probability. Preserve underlying public market research functionality and local Ollama signed-in transport (no new keys). Expose actual model used, fallback when known, answer time and data provenance. Never automatically send portfolio/positions/notes/private chat history to web search. Current raw user question goes to search: disclose it clearly before user sends AI research questions.

3. THREAD/ASYNC CORRECTNESS: LLM assistant messages currently omit ticker while user messages include it. Persist both to the same thread. Pass bounded same-thread conversational context for meaningful follow-ups to model (clearly disclosed external processing), not other threads or financial portfolio. UI must not display an in-flight response/snapshot for ticker A in ticker B. Guard late responses when switching stocks/threads. Show retry/error states, clear visible and not silently succeeding on failure; prevent duplicate sends. Preserve existing messages, distinguish legacy unverified answers by timestamp/provenance rather than deleting them.

4. JOURNAL: current trackRecord.ts uses current price +/-0.5% to call advice right/wrong with hit rate. This is not a benchmark. Replace UI headline/metrics with Research journal and descriptive price change since observation, timestamps, model and hypothesis, research-only/not strategy performance. Do not label avoid as a profitable short or promote arbitrary holding periods to realized returns. Preserve all old logs. Log ticker-specific research outputs consistently (including ticker-thread LLM replies) with source availability/time and model, without parsing arbitrary prose into fabricated numeric recommendations. No fake scores for general chat. Track fixed-horizon outcomes only if actually defensible data exists; otherwise clearly pending/unmeasured. Do not spend this milestone inventing backtests.

5. TEST/SECURITY: remove real-network/model dependencies from npm test and smoke via dependency injection/mock fetch or purpose-built isolated fixtures. Cover disabled broker endpoints for every method and hostile callback HTML (static JSON), no broker requests, missing/stale/failing data, citation validation, model unavailable, LLM thread persistence, guard regressions, journal no fake hit rate, cached replies source timestamps, source parse failures. Preserve Host validation for reads and mutation Origin/JSON controls. No API secrets in config responses; use provider booleans/model labels only. GET broker disable must work without credential reads. Smoke defaults isolated storage and must not read ~/.civicfolio/env. Add CI using npm ci, tests, typecheck, build with no secrets/network models. Do not weaken assertions just to pass. Existing test suite has no real-order tests; replace obsolete claims rather than claiming those validate execution. No global browser/network permissions changes.

6. FINISH: npm test, typecheck, build, isolated smoke. Update README.md and HANDOFF.md to one current accurate source of truth, archive obsolete handoff narrative separately only if valuable. Document actual runtime, localhost URL, how to start, data sources, failure/retry, model routing/privacy, research limitations, what remains unsupported and that local Robinhood credentials removed but provider-side grant revocation not verified. Preserve launchd service and storage; do not edit login settings. Tell Astra how to restart exact service safely; do not kill unrelated servers. Report exact tests, changed files, known gaps. Stop after implementation/verification; Astra runs real UI/one research query and commits.

Aim for one working useful research product, not another expanding gauntlet. Prioritize brokerage removal, honest research, working threads and tests over cosmetic rewrites.
