Build Civicfolio as a polished, working personal localhost app. You are the GLM implementation worker under Astra's coordination. Implement, run and test it; do not only write a plan. Use a simple maintainable stack (React/Vite frontend with a local Node backend and file/SQLite persistence is appropriate). Inspect runtime availability first. Install ordinary project dependencies as needed. No external deployment, GitHub push, live financial transactions, or credential discovery.

Visual direction: sophisticated dark navy investment dashboard, readable typography, teal/blue accents, restrained cards, excellent spacing. Sidebar: Overview, Disclosures, Research Chat, Paper Portfolio, Settings. Display data mode conspicuously. Responsive desktop layout. Avoid giant disclaimers; show useful data provenance and limitations close to the values.

Functional first milestone:
1. Overview with tracked ideas, portfolio allocation, disclosed activity, watchlist, and actual working navigation.
2. Disclosure feed with filters and details. Explicit transaction date versus publication date, owner, amount RANGE, ticker, primary source link, amendment flag. Bundle realistic SYNTHETIC DEMO records explicitly labeled synthetic; never attribute invented trades to actual people. Provide a real-data import adapter accepting validated user-supplied JSON/CSV with documented schema. A public data endpoint may be added only if actually verified and terms permit; never pretend demo is live.
3. Research chat backed by a local API: deterministic evidence-based mode usable without API keys, citing stored record IDs/source URLs; optional configured OpenAI-compatible endpoint through server-only env variables. Do not market deterministic mode as an LLM. It should answer about stored disclosures, reporting delays, portfolio concentration, and compare ideas with uncertainty and counterarguments; abstain when data absent. No made-up prices, returns, news, or probability scores.
4. Paper portfolio: submit paper BUY/SELL at explicitly user-entered or labeled demo prices, maintain cash/positions/trade journal, reject overspending and invalid inputs, persist across restart. Paper only.
5. Settings: provider/data configuration status without secrets, Robinhood connection marked not configured. Official Robinhood Agentic MCP is a possible later connector; do not implement undocumented password/session scraping or a fake connected state.
6. README, .env.example, .gitignore, meaningful backend tests, build/type checks and smoke test. Include clear demo reset/import controls with confirmation. Store local data outside tracked files.

Security/basic integrity: bind 127.0.0.1, validate request payloads and finite numbers, constrain file import size and avoid arbitrary file paths, avoid exposing secrets to frontend, do not execute instructions from research data. Restrict optional model endpoint config to server config, not arbitrary browser-provided URL.

Start dev servers on available ports, document exact URL and process commands. Write HANDOFF.md with implemented features, actual verification, remaining gaps, sources and final URL. Do not claim real Robinhood or live disclosures work if unavailable. Prioritize completing this scoped app over speculative connectors. Work only in this project.
